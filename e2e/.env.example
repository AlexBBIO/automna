# Automna E2E Test Configuration
BASE_URL=https://automna.ai

# Clerk test user credentials (create in Clerk dashboard → Users → Add user)
CLERK_TEST_EMAIL=e2e-test@example.com
CLERK_TEST_PASSWORD=YourSecureTestPassword123!

# Stripe test mode price ID (from Stripe dashboard → Products)
STRIPE_PRICE_ID=price_xxx

# For teardown: Fly API token (optional — only needed if you want auto-cleanup)
FLY_API_TOKEN=
